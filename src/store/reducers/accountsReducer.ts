import * as ActionTypes from "../types";

const initialState = {
}

export default function accountsReducer(
    state = initialState,
    action
) {
        default:
            return state;
    }
}