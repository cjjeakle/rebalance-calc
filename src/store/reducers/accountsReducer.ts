import * as ActionTypes from "../actions/types";

const initialState = {
}

export default function accountsReducer(
    state = initialState,
    action
) {
        default:
            return state;
    }
}